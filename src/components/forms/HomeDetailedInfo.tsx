import { QuoteFormProps } from "../../types/types";

const HomeDetailedInfo: React.FC<QuoteFormProps> = ({
  setFormData,
  setActiveStep,
  formData,
}) => {
  return <div>HomeDetailedInfo</div>;
};

export default HomeDetailedInfo;
