import { QuoteFormProps } from "../../../../types/types";

const QuoteStepTwo: React.FC<QuoteFormProps> = ({
  setFormData,
  setActiveStep,
  formData,
}) => {
  return <div>QuoteStepTwo</div>;
};

export default QuoteStepTwo;
