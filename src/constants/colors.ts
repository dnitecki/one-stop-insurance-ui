export const PRIMARY_RED = getComputedStyle(
  document.documentElement
).getPropertyValue("--primary-red");

export const WHITE = getComputedStyle(
  document.documentElement
).getPropertyValue("--white");
